#include<stdio.h>
int main()
{
	int n,a,sum=0,k;
	printf("enter the number= ");
	scanf("%d",&n);
	k=n;
	while(n>0)
	{
		a=n%10;
		sum=sum+(a*a*a);
		n=n/10;
	}
	if(k==sum)
	{
		printf("ARMSTRONG NUMBER");
	}
	else
	{
		printf("NOT AN  ARMSTRONG NUMBER");
	}
}
