#include<stdio.h>
int main()
{
	int n,i,m;
	printf("enter the number of elements in an array= ");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	{
		printf("enter the number= ");
		scanf("%d",&a[i]);
	}
	m=a[0];
	for(i=1;i<n;i++)
	{
		if(m<a[i])
		{
			m=a[i];
		}
	}
	printf("%d",m);
}
